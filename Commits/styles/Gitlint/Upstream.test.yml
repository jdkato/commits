# Generated by script/upstream.py from gitlint's own test fixtures:
# https://github.com/jorisroovers/gitlint/blob/4d9119760056/gitlint-core/gitlint/tests/test_lint.py
# Left out: sample4 and sample5, which carry gitlint's in-message `gitlint-ignore` directives, and M1, which reads the author.

- name: sample1-TitleTrailingPunctuation
  about: "sample1: gitlint reports T3"
  format: Gitlint
  input: "Commit title contåining 'WIP', as well as trailing punctuation.\nThis line should be empty\nThis is the first line of the commit message body and it is meant to test a line that exceeds the maximum line length of 80 characters.\nThis line has a tråiling space. \nThis line has a trailing tab.\t\n# This is a cömmented  line\n# ------------------------ >8 ------------------------\n# Anything after this line should be cleaned up\n# this line appears on `git commit -v` command\ndiff --git a/gitlint/tests/samples/commit_message/sample1 b/gitlint/tests/samples/commit_message/sample1\nindex 82dbe7f..ae71a14 100644\n--- a/gitlint/tests/samples/commit_message/sample1\n+++ b/gitlint/tests/samples/commit_message/sample1\n@@ -1 +1 @@\n"
  contains: "Gitlint.TitleTrailingPunctuation"

- name: sample1-TitleMustNotContainWord
  about: "sample1: gitlint reports T5"
  format: Gitlint
  input: "Commit title contåining 'WIP', as well as trailing punctuation.\nThis line should be empty\nThis is the first line of the commit message body and it is meant to test a line that exceeds the maximum line length of 80 characters.\nThis line has a tråiling space. \nThis line has a trailing tab.\t\n# This is a cömmented  line\n# ------------------------ >8 ------------------------\n# Anything after this line should be cleaned up\n# this line appears on `git commit -v` command\ndiff --git a/gitlint/tests/samples/commit_message/sample1 b/gitlint/tests/samples/commit_message/sample1\nindex 82dbe7f..ae71a14 100644\n--- a/gitlint/tests/samples/commit_message/sample1\n+++ b/gitlint/tests/samples/commit_message/sample1\n@@ -1 +1 @@\n"
  contains: "Gitlint.TitleMustNotContainWord"

- name: sample1-Blank
  about: "sample1: gitlint reports B4"
  format: Gitlint
  input: "Commit title contåining 'WIP', as well as trailing punctuation.\nThis line should be empty\nThis is the first line of the commit message body and it is meant to test a line that exceeds the maximum line length of 80 characters.\nThis line has a tråiling space. \nThis line has a trailing tab.\t\n# This is a cömmented  line\n# ------------------------ >8 ------------------------\n# Anything after this line should be cleaned up\n# this line appears on `git commit -v` command\ndiff --git a/gitlint/tests/samples/commit_message/sample1 b/gitlint/tests/samples/commit_message/sample1\nindex 82dbe7f..ae71a14 100644\n--- a/gitlint/tests/samples/commit_message/sample1\n+++ b/gitlint/tests/samples/commit_message/sample1\n@@ -1 +1 @@\n"
  contains: "Commits.Blank"

- name: sample1-BodyMaxLineLength
  about: "sample1: gitlint reports B1"
  format: Gitlint
  input: "Commit title contåining 'WIP', as well as trailing punctuation.\nThis line should be empty\nThis is the first line of the commit message body and it is meant to test a line that exceeds the maximum line length of 80 characters.\nThis line has a tråiling space. \nThis line has a trailing tab.\t\n# This is a cömmented  line\n# ------------------------ >8 ------------------------\n# Anything after this line should be cleaned up\n# this line appears on `git commit -v` command\ndiff --git a/gitlint/tests/samples/commit_message/sample1 b/gitlint/tests/samples/commit_message/sample1\nindex 82dbe7f..ae71a14 100644\n--- a/gitlint/tests/samples/commit_message/sample1\n+++ b/gitlint/tests/samples/commit_message/sample1\n@@ -1 +1 @@\n"
  contains: "Gitlint.BodyMaxLineLength"

- name: sample1-BodyTrailingWhitespace
  about: "sample1: gitlint reports B2"
  format: Gitlint
  input: "Commit title contåining 'WIP', as well as trailing punctuation.\nThis line should be empty\nThis is the first line of the commit message body and it is meant to test a line that exceeds the maximum line length of 80 characters.\nThis line has a tråiling space. \nThis line has a trailing tab.\t\n# This is a cömmented  line\n# ------------------------ >8 ------------------------\n# Anything after this line should be cleaned up\n# this line appears on `git commit -v` command\ndiff --git a/gitlint/tests/samples/commit_message/sample1 b/gitlint/tests/samples/commit_message/sample1\nindex 82dbe7f..ae71a14 100644\n--- a/gitlint/tests/samples/commit_message/sample1\n+++ b/gitlint/tests/samples/commit_message/sample1\n@@ -1 +1 @@\n"
  contains: "Gitlint.BodyTrailingWhitespace"

- name: sample1-HardTab
  about: "sample1: gitlint reports B3"
  format: Gitlint
  input: "Commit title contåining 'WIP', as well as trailing punctuation.\nThis line should be empty\nThis is the first line of the commit message body and it is meant to test a line that exceeds the maximum line length of 80 characters.\nThis line has a tråiling space. \nThis line has a trailing tab.\t\n# This is a cömmented  line\n# ------------------------ >8 ------------------------\n# Anything after this line should be cleaned up\n# this line appears on `git commit -v` command\ndiff --git a/gitlint/tests/samples/commit_message/sample1 b/gitlint/tests/samples/commit_message/sample1\nindex 82dbe7f..ae71a14 100644\n--- a/gitlint/tests/samples/commit_message/sample1\n+++ b/gitlint/tests/samples/commit_message/sample1\n@@ -1 +1 @@\n"
  contains: "Gitlint.HardTab"

- name: sample2-TitleMustNotContainWord
  about: "sample2: gitlint reports T5"
  format: Gitlint
  input: |
    Just a title contåining WIP
  contains: "Gitlint.TitleMustNotContainWord"

- name: sample2-BodyIsMissing
  about: "sample2: gitlint reports B6"
  format: Gitlint
  input: |
    Just a title contåining WIP
  contains: "Gitlint.BodyIsMissing"

- name: sample3-TitleMaxLength
  about: "sample3: gitlint reports T1"
  format: Gitlint
  input: " Commit title containing 'WIP', \tleading and tråiling whitespace and longer than 72 characters.\nThis line should be empty\nThis is the first line is meånt to test a line that exceeds the maximum line length of 80 characters.\nThis line has a trailing space. \nThis line has a tråiling tab.\t\n# This is a commented  line\n"
  contains: "Gitlint.TitleMaxLength"

- name: sample3-TitleTrailingPunctuation
  about: "sample3: gitlint reports T3"
  format: Gitlint
  input: " Commit title containing 'WIP', \tleading and tråiling whitespace and longer than 72 characters.\nThis line should be empty\nThis is the first line is meånt to test a line that exceeds the maximum line length of 80 characters.\nThis line has a trailing space. \nThis line has a tråiling tab.\t\n# This is a commented  line\n"
  contains: "Gitlint.TitleTrailingPunctuation"

- name: sample3-HardTab
  about: "sample3: gitlint reports T4, B3"
  format: Gitlint
  input: " Commit title containing 'WIP', \tleading and tråiling whitespace and longer than 72 characters.\nThis line should be empty\nThis is the first line is meånt to test a line that exceeds the maximum line length of 80 characters.\nThis line has a trailing space. \nThis line has a tråiling tab.\t\n# This is a commented  line\n"
  contains: "Gitlint.HardTab"

- name: sample3-TitleMustNotContainWord
  about: "sample3: gitlint reports T5"
  format: Gitlint
  input: " Commit title containing 'WIP', \tleading and tråiling whitespace and longer than 72 characters.\nThis line should be empty\nThis is the first line is meånt to test a line that exceeds the maximum line length of 80 characters.\nThis line has a trailing space. \nThis line has a tråiling tab.\t\n# This is a commented  line\n"
  contains: "Gitlint.TitleMustNotContainWord"

- name: sample3-Whitespace
  about: "sample3: gitlint reports T6"
  format: Gitlint
  input: " Commit title containing 'WIP', \tleading and tråiling whitespace and longer than 72 characters.\nThis line should be empty\nThis is the first line is meånt to test a line that exceeds the maximum line length of 80 characters.\nThis line has a trailing space. \nThis line has a tråiling tab.\t\n# This is a commented  line\n"
  contains: "Commits.Whitespace"

- name: sample3-Blank
  about: "sample3: gitlint reports B4"
  format: Gitlint
  input: " Commit title containing 'WIP', \tleading and tråiling whitespace and longer than 72 characters.\nThis line should be empty\nThis is the first line is meånt to test a line that exceeds the maximum line length of 80 characters.\nThis line has a trailing space. \nThis line has a tråiling tab.\t\n# This is a commented  line\n"
  contains: "Commits.Blank"

- name: sample3-BodyMaxLineLength
  about: "sample3: gitlint reports B1"
  format: Gitlint
  input: " Commit title containing 'WIP', \tleading and tråiling whitespace and longer than 72 characters.\nThis line should be empty\nThis is the first line is meånt to test a line that exceeds the maximum line length of 80 characters.\nThis line has a trailing space. \nThis line has a tråiling tab.\t\n# This is a commented  line\n"
  contains: "Gitlint.BodyMaxLineLength"

- name: sample3-BodyTrailingWhitespace
  about: "sample3: gitlint reports B2"
  format: Gitlint
  input: " Commit title containing 'WIP', \tleading and tråiling whitespace and longer than 72 characters.\nThis line should be empty\nThis is the first line is meånt to test a line that exceeds the maximum line length of 80 characters.\nThis line has a trailing space. \nThis line has a tråiling tab.\t\n# This is a commented  line\n"
  contains: "Gitlint.BodyTrailingWhitespace"

- name: no-violations
  about: "no-violations: clean in gitlint"
  format: Gitlint
  input: |
    Normal Commit Tïtle

    Nörmal body that contains a few lines of text describing the changes in the
    commit without violating any of gitlint's rules.

    Sïgned-Off-By: foo@bar.com
  want: ""
