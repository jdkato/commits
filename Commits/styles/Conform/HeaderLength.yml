# `header.length`, 89 by default. `Conform.HeaderLength[max]` sets it.
extends: Commits._shared.SubjectLength
message: "Header is %d characters."
link: https://github.com/siderolabs/conform#commit-policy
level: error
max: 89

tests:
  - name: "before"
    about: "the README's sample policy, most checks failing at once"
    format: COMMIT_EDITMSG
    view: Commit
    input: |
      Added the parser, in a header that runs past the eighty-nine characters that conform allows.

      Signed-off-by: J
    want: |
      1:1:Conform.HeaderLength:Header is 92 characters.
  - name: "after"
    about: "the rewrite"
    format: COMMIT_EDITMSG
    view: Commit
    input: |
      add the parser

      The parser reads the file into a tree that the rest of the tool
      walks.

      Signed-off-by: Jane Doe <jane@example.com>
    want: ""
