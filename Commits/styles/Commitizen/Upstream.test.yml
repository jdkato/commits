# Generated by script/upstream.py from commitizen's own test fixtures:
# https://github.com/commitizen-tools/commitizen/blob/9432748e110b/tests/commands/test_check_command.py
# Left out: the empty message, which Git aborts before a hook runs, and the range and prefix options, which are the hook's.

- name: valid-1
  about: "passes cz check"
  format: Commitizen
  input: |
    feat(lang)!: removed polish language
  absent:
    - "Conventional."
    - "Commitizen."
    - "Commits.Blank"

- name: valid-2
  about: "passes cz check"
  format: Commitizen
  input: |
    feat(lang): added polish language
  absent:
    - "Conventional."
    - "Commitizen."
    - "Commits.Blank"

- name: valid-3
  about: "passes cz check"
  format: Commitizen
  input: |
    feat: add polish language
  absent:
    - "Conventional."
    - "Commitizen."
    - "Commits.Blank"

- name: valid-4
  about: "passes cz check"
  format: Commitizen
  input: |
    bump: 0.0.1 -> 1.0.0
  absent:
    - "Conventional."
    - "Commitizen."
    - "Commits.Blank"

- name: valid-5
  about: "passes cz check"
  format: Commitizen
  input: |
    fix(scope): some commit message
  absent:
    - "Conventional."
    - "Commitizen."
    - "Commits.Blank"

- name: valid-6
  about: "passes cz check"
  format: Commitizen
  input: |
    # <type>: (If applied, this commit will...) <subject>
    # |<---- Try to Limit to a Max of 50 char ---->|
    ci: add commitizen pre-commit hook

    # Explain why this change is being made
    # |<---- Try To Limit Each Line to a Max Of 72 Char ---->|
    This pre-commit hook will check our commits automatically.
  absent:
    - "Conventional."
    - "Commitizen."
    - "Commits.Blank"

- name: invalid-1
  about: "fails cz check"
  format: Commitizen
  input: |
    feat!(lang): removed polish language
  contains: "Conventional.Type"

- name: invalid-2
  about: "fails cz check"
  format: Commitizen
  input: |
    no conventional commit
  contains: "Conventional.Type"

- name: invalid-3
  about: "fails cz check"
  format: Commitizen
  input: |
    ci: check commit message on merge
    testing with more complex commit mes

    age with error
  contains: "Commits.Blank"

- name: invalid-4
  about: "fails cz check"
  format: Commitizen
  input: |
    bad commit
  contains: "Conventional.Type"
