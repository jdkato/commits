# Generated by script/upstream.py from committed's own test fixtures:
# https://github.com/crate-ci/committed/blob/e5599f97c052/crates/committed/tests/cmd.rs
# Left out: the empty message, and the cases that set a config key per run.

- name: cmd-2
  about: "committed: error Work-in-progress commits must be cleaned up"
  format: Committed
  input: |
    wip bad times ahead
  contains: "Committed.NoWip"

- name: cmd-4
  about: "committed: error Work-in-progress commits must be cleaned up"
  format: Committed
  input: |
    wip bad times
  contains: "Committed.NoWip"

- name: cmd-5
  about: "committed: error Fixup commits must be squashed"
  format: Committed
  input: |
    fixup! bad times ahead
  contains: "Committed.NoFixup"

- name: cmd-7
  about: "committed: error Fixup commits must be squashed"
  format: Committed
  input: |
    fixup! bad times
  contains: "Committed.NoFixup"
